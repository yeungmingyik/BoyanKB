# BoyanKB 版本路线

当前预发布版本：`0.1.0-alpha.3`。飞书同步、系统内阅读和原生索引已交付，合成场景集成通过；首次整空间发现 18 份资料，12 份已发布、6 份保持未发布。最终镜像的 HTTP、Worker、正式部署阅读与索引核对通过；知识检索和问答在 alpha.4 开发。验收结果以 [验收清单](acceptance.md) 为准。

## 1. 版本目标

| 版本          | 交付范围                                                                                                               | 完成门槛                                                                                                                             |
| ------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 0.1.0-alpha.1 | 锁定 LibreChat 基线；产品需求、开发约束、架构、验收标准、产品 README 和版本管理                                        | 基线提交可追溯，规划文件一致，无将计划写成已实现的记录                                                                               |
| 0.1.0-alpha.2 | 本地网页运行；原生账号与定向邀请；关闭公开注册；知识 Agent 逐用户 `VIEW`；封禁、撤权、私有会话；多供应商和个人模型配置 | AC-01 至 AC-07、AC-31、AC-32、AC-35；相关入口统一检查 `VIEW` 与 `checkBan`，撤权清理刷新会话，禁用普通用户创建 Agent、分享和上传旁路 |
| 0.1.0-alpha.3 | `whole_space` 完整发现；同步台账；全量与增量同步；资料目录与系统内原文预览；重试与删除处理                             | AC-08 至 AC-16、AC-18、AC-34；完整盘点嵌入对象和实际格式缺口                                                                         |
| 0.1.0-alpha.4 | 关键词与语义检索；多供应商业务问答；版本一致的系统内引用；资料不足和冲突处理；撤权或封禁时停止新请求与在途输出         | AC-19 至 AC-24、AC-33、AC-36；问答验收集达到门槛；新增接口权限及撤权验证通过                                                         |
| 0.1.0-beta.1  | 授权伙伴网页试点；实际网络访问；移动端关键流程；性能基线；公司模型网关额度和停用                                       | AC-25、AC-26、AC-30、AC-37；试点网络、账号范围、资料覆盖和设备指标明确                                                               |
| 0.1.0         | 本地正式运行；实际格式覆盖；持续运行；备份恢复；升级回退                                                               | AC-01 至 AC-37 全部通过，无未解决的越权、资料丢失或错误发布缺陷                                                                      |
| 0.2.0         | 云服务器部署、HTTPS 入口、持久化数据迁移、备份与运行监控                                                               | 云端权限与访问验证、迁移数据核对、恢复演练和版本回退通过                                                                             |

## 2. 首期实施顺序

1. 使用原生 `create-user` 建号；关闭自主注册和社交注册，保留邮箱登录；需要定向邀请时配置 SMTP 并使用 `invite-user`。管理员持有知识 Agent，通过原生分享界面逐用户授予 `VIEW`；禁用普通用户创建 Agent、分享和上传。
2. 建立可重复启动的本地环境，相关入口统一检查 Agent `VIEW` 与 `checkBan`；移除 `VIEW` 执行永久撤权，原生限时封禁用于临时停用；清理刷新会话并完成账号权限与个人会话验收。
3. 从配置入口解析整个知识空间，建立稳定资料标识、逐项同步状态及嵌入对象清单。
4. 完成文档和实际附件适配、版本发布与系统内预览，验证重试与下线行为。
5. 接入检索与问答，复用原生聊天入口，将引用接入系统内预览。
6. 在确定的目标网络开展伙伴试点，记录真实资料覆盖、使用缺陷和设备负载。
7. 完成格式覆盖、数据恢复和升级演练后发布本地正式版本。

## 3. 决策节点

| 决策                   | 期限             | 默认边界                                                                                                       |
| ---------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------- |
| 原生管理能力与权限覆盖 | alpha.2 开发前   | 使用 `ADMIN`、`USER`、原生命令和原生 Agent 分享界面；核验并补齐 `VIEW` 与 `checkBan` 检查，不建设独立伙伴后台  |
| 资料格式与对象范围     | alpha.3 验收前   | 完整抽取后确认，不能依据节点均为文档推断全部内容可解析                                                         |
| 嵌入服务与 RAG 版本    | alpha.3 集成验收 | 本地 BAAI/bge-small-zh-v1.5，修订 7999e1d、512 维；RAG 与 pgvector 固定镜像 digest，验证文件入库和离线向量计算 |
| 问答模型               | alpha.4 验收前   | 伙伴自选供应商与个人密钥，DeepSeek 用于首轮真实测试；公司服务采用伙伴专属网关密钥                              |
| 伙伴网络入口           | beta.1 试点前    | 开发使用 localhost；远程访问方案待定，VPN 未确认                                                               |
| 运行规格与服务指标     | beta.1 试点前    | 基于实际 PC、模型、语料和并发规模确定，不预填未经测量的性能结果                                                |
| 云端部署规格           | 0.2.0 开发前     | 依据本地正式版使用规模与恢复要求确定                                                                           |

## 4. 版本管理

- BoyanKB 产品版本与 LibreChat 上游包版本分别维护。
- 使用语义化版本，预发布标识采用 `alpha`、`beta` 及递增序号。
- 产品版本、Git 标签和更新日志一一对应；标签采用 `v<产品版本>`。
- 更新日志只记录该版本实际交付的新增、变更和修复；未完成事项保留在路线中。
- 分支使用实际功能命名，例如 `feishu-sync`、`authorized-access`、`knowledge-search`，不使用 `codex/` 前缀。
- 提交信息遵循 Conventional Commits，提交身份使用仓库所有者配置，不增加工具或代理署名。
- 上游更新先核对差异与兼容性，再更新锁定提交并执行受影响范围的验收。
- 未通过版本门槛不得标记为正式发布。

## 5. 后续候选

alpha.3 已完成读取与服务组件的 HTTP、原生 RAG、浏览器及网页重试复验，以及实际空间 18 份资料的覆盖盘点。最终镜像的 HTTP、Worker、正式部署阅读与索引核对通过；实际全格式可用性尚未通过 AC-17。

| 实际资料缺口                         | 数量 | 后续处理                                                                         |
| ------------------------------------ | ---- | -------------------------------------------------------------------------------- |
| 源正文为空，且无媒体、子节点         | 4    | 由资料负责人在飞书补充有效正文后重试；为空时保持未发布，不生成伪造的可检索正文   |
| 成员提及仅含用户标识与样式、无显示名 | 2    | 完成成员提及的安全文本解析后重试；保持未发布，不推测姓名或向伙伴展示原始用户标识 |

具体状态见[资料覆盖清单](source-coverage.md)。自动物理清理、备份恢复与持续运行留在正式版验收范围；资料下线访问控制不依赖物理清理。

伙伴标签、细分知识授权、多知识源、飞书写回、报名与付费、活动运营及申请办理不属于首期。多供应商 BYOK 纳入 alpha.2 的模型入口与凭据隔离，跨供应商知识问答在 alpha.4 验收。公司提供的模型服务按伙伴专属网关密钥接入，在 beta.1 验证额度和停用；在线收款后续安排。
