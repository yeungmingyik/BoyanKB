# BoyanKB 产品需求

| 项目         | 要求                                                                    |
| ------------ | ----------------------------------------------------------------------- |
| 产品形态     | 浏览器访问的企业知识库                                                  |
| 当前版本     | 0.1.0-alpha.3 开发版                                                    |
| 当前能力     | 本地部署、原生账号授权、多供应商入口、飞书同步与资料阅读                |
| 功能状态     | 同步与阅读待集成验收；知识检索与问答在 alpha.4 开发，验证范围见验收清单 |
| 首期目标     | 本地 PC 运行，员工和已授权合作伙伴使用                                  |
| 后续目标     | 迁移到云服务器                                                          |
| 上游基线     | LibreChat `main`，`7b2362d7a7c6148b84850924dc7fa5fc43307923`            |
| 上游应用版本 | `0.8.8-rc4`，独立于 BoyanKB 产品版本                                    |

## 1. 产品目标

员工和已授权合作伙伴通过网页浏览资料、检索知识和进行带来源的业务问答。指定飞书知识空间为唯一知识事实源，资料同步到 BoyanKB 后提供服务。合作伙伴无需飞书账号或知识空间访问权限。

所有已授权用户共享同一知识池。管理员持有一个知识 Agent，通过原生分享界面逐用户授予 `VIEW`；不得通过 `USER` 角色整体授予该 Agent 的 `VIEW`。知识访问同时要求登录有效、未被封禁且拥有该 Agent 的 `VIEW`。个人会话仅对所有者可见。首期不建设独立伙伴管理系统，不引入伙伴组织分级授权。

优先复用 LibreChat 的登录、账号管理命令、角色、封禁、聊天、个人历史和现有展示组件。仅扩展飞书同步、资料预览、知识检索及必要的权限校验。

伙伴可自主选择模型供应商，使用自行购买的 API 密钥，或购买公司提供的模型服务。复用 LibreChat 原生模型选择和个人密钥管理。知识权限与模型服务分离；知识 Agent 作为原生授权与资料资源，不要求伙伴获得 Agent 编辑权限。

## 2. 业务范围

| 业务分类         | 资料范围                                                                              |
| ---------------- | ------------------------------------------------------------------------------------- |
| 中小学科创教育   | 机器人、无人机、编程、AI 课程体系，课程大纲、教案、材料清单、师资要求、实施方案和案例 |
| 成人 AI 培训     | 培训方案、学习目标、教学材料、练习、企业培训交付方案                                  |
| 中小学 AI 营地   | 企业参访、AI 知识学习、作品制作、营地流程、执行清单和服务标准                         |
| 中小学 AI 黑客松 | 活动方案、挑战题目、导师安排、活动规则、成果展示、作品保护和申请资料                  |
| 经营与交付支持   | 产品资料、合作方案、报价政策、交付流程、常见问题和模板                                |
| 其他业务         | 新增业务与待分类资料                                                                  |

保留飞书目录结构。业务分类用于浏览与检索，不改变共享知识范围。无法确定分类的资料进入待分类集合，不阻断发布。

作品保护相关能力限于资料查询和已有流程支持；首期不提交申请、不办理登记、不生成办理成功或取得权利的结论。

## 3. 用户与权限

| 能力                                        | 管理员 `ADMIN` | 员工 `USER` | 合作伙伴 `USER` |
| ------------------------------------------- | -------------- | ----------- | --------------- |
| 登录并使用共享知识                          | 允许           | 允许        | 允许            |
| 浏览、检索和问答                            | 允许           | 允许        | 允许            |
| 管理自己的会话                              | 允许           | 允许        | 允许            |
| 阅读其他用户会话                            | 不提供         | 不提供      | 不提供          |
| 创建账号、定向邀请、封禁账号                | 允许           | 不允许      | 不允许          |
| 通过原生分享界面授予或移除知识 Agent `VIEW` | 允许           | 不允许      | 不允许          |
| 执行同步、检查失败、重试                    | 允许           | 不允许      | 不允许          |
| 修改连接、模型和部署配置                    | 允许           | 不允许      | 不允许          |
| 选择已接入模型、管理自己的供应商密钥        | 允许           | 允许        | 允许            |

- 禁止未授权用户自主注册；首期使用原生 `create-user` 命令创建账号，无需 SMTP。
- 设置 `ALLOW_REGISTRATION=false`、`ALLOW_EMAIL_LOGIN=true`、`ALLOW_SOCIAL_REGISTRATION=false`；配置 SMTP 后可通过原生 `invite-user` 命令发送定向邀请，有效邀请允许完成注册。
- 员工与合作伙伴使用同一原生普通用户角色；伙伴标签为后续可选元数据。
- 对新增知识 API 及相关会话、文件入口统一校验原生 Agent `VIEW` 与 `checkBan`，不能只检查是否登录。
- 原生 `ban-user` 用于限时封禁；永久撤销知识权限使用移除知识 Agent `VIEW`。
- 撤权或封禁后清除相关刷新会话，现有 JWT 不得绕过知识权限检查；新检索、新问答和在途输出必须停止。
- 禁用 `USER` 的 Agent 创建、分享和上传能力，阻止通过其他入口访问未授权知识。
- 禁止公开会话分享，不提供跨用户会话浏览和组织级会话共享。
- 管理操作优先使用原生命令或已有入口，不以独立管理站点作为首期交付条件。

## 4. 首期需求

下表全部为 P0，交付范围按目标版本划分，验证结果见验收清单。

| 编号  | 需求            | 完成条件                                                                                                     | 目标版本                 |
| ----- | --------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------ |
| BK-01 | 授权访问        | 管理员建号、禁止自主注册、知识 Agent 逐用户 `VIEW`、封禁与撤权通过验证；不具备权限的已有会话不得继续获取知识 | 0.1.0-alpha.2            |
| BK-02 | 个人会话        | 复用原生聊天与历史；用户只能操作自己的会话                                                                   | 0.1.0-alpha.2            |
| BK-03 | 整空间发现      | 从环境配置的入口解析所属空间，递归发现整个空间内的资料                                                       | 0.1.0-alpha.3            |
| BK-04 | 同步与发布      | 首次全量、定时增量、手动触发、失败重试、变更和删除处理可用                                                   | 0.1.0-alpha.3            |
| BK-05 | 系统内阅读      | 用户可在网页中按目录找到已发布资料并打开原文预览                                                             | 0.1.0-alpha.3            |
| BK-06 | 格式覆盖        | 全部发现资料及嵌入对象有处理状态；正式版前完成实际资料类型的阅读和检索验收                                   | 0.1.0                    |
| BK-07 | 知识检索        | 关键词、语义检索及目录或分类筛选返回已发布资料                                                               | 0.1.0-alpha.4            |
| BK-08 | 知识问答        | 答案基于已发布资料，引用可打开对应系统内原文；撤权或封禁后停止新请求及在途输出                               | 0.1.0-alpha.4            |
| BK-09 | 资料不足处理    | 对缺失信息、冲突资料和未知办理结果采用明确的受限回答                                                         | 0.1.0-alpha.4            |
| BK-10 | 同步运维        | 管理员可查询覆盖率、最近成功时间、失败原因并重试；入口可为受控命令                                           | 0.1.0-alpha.3            |
| BK-11 | 伙伴网页试点    | 已授权伙伴在目标网络通过浏览器使用；未授权请求被拒绝                                                         | 0.1.0-beta.1             |
| BK-12 | 本地持续运行    | 持久化、重启恢复、备份恢复和版本升级可执行                                                                   | 0.1.0                    |
| BK-13 | 多供应商与 BYOK | 原生模型选择、个人密钥隔离、DeepSeek 与兼容 API 配置可用；所有供应商共用知识授权与检索范围                   | 0.1.0-alpha.2 至 alpha.4 |
| BK-14 | 公司模型服务    | 支持公司发放的伙伴专属网关密钥；额度和停用由网关执行，不把公司主密钥发给伙伴                                 | 0.1.0-beta.1             |

## 5. 知识源与同步规则

### 5.1 数据边界

- 知识源范围固定为 `whole_space`，不以入口节点的子树替代整个空间。
- 知识空间定位、入口标识、应用凭据和模型密钥由本地环境配置提供。
- 仓库及公开测试材料不得包含真实知识空间地址、资料标题、正文、令牌或生产账号信息。
- 飞书为内容更新入口。首期不在 BoyanKB 编辑源资料或写回飞书。
- 不允许普通用户上传资料进入共享知识池，也不接入网络搜索作为业务答案来源。

### 5.2 发布与版本

- 使用稳定源标识识别资料，保留源版本或更新时间、目录、同步时间和发布版本。
- 相同版本重复同步不得生成重复资料、附件或检索记录。
- 重命名和空间内移动更新原有资料，不创建副本。
- 新版本完成抽取与索引后再替换已发布版本；预览、检索和引用使用一致版本。
- 解析失败不得发布空白新版本；管理员能够识别旧版本仍在服务及其更新时间。
- 仅在成功核对后将已删除或移出空间的资料下线。
- 分页中断、超时、鉴权失败和限流不得推断资料已删除。
- 资料下线后不得继续出现在检索结果、问答检索或原文预览中；历史会话的相关引用显示不可用，不跳转到其他资料。

### 5.3 覆盖与格式

分别统计已发现、可阅读和可检索的资料数。每项资料必须有明确状态：待处理、处理完成、处理失败、无权限、不支持或已下线。空间元数据盘点不等同于正文和嵌入对象覆盖完成。

| 内容类型           | 要求                                                                       |
| ------------------ | -------------------------------------------------------------------------- |
| 文档正文           | 保留标题层级、段落、列表和可用表格，并提供可定位的文本片段                 |
| 图片               | 提供授权后的系统内预览；未完成识别时不得将图片内容计为可检索               |
| 文档附件           | 根据实际格式提供下载后的解析和系统内阅读；原文件下载入口不作为首期默认能力 |
| 电子表格、多维表格 | 单独记录适配与覆盖状态，不作为普通正文自动计入完成                         |
| 画板、音视频、外链 | 明确处理能力与缺口，不得静默忽略                                           |

所有暂不支持的实际内容必须进入适配清单。存在未覆盖资料或嵌入对象时，不得标记为整空间资料全部可用。

## 6. 问答与引用

- 业务结论必须得到对应原文支持；引用展示资料标题、源更新时间或发布版本以及定位片段。
- 引用打开 BoyanKB 内预览，不能要求合作伙伴登录飞书。
- 资料不足时明确返回未找到相关内容，不补造价格、优惠、课程时长、合作政策或活动安排。
- 资料冲突时展示不同来源及其时间，不擅自认定现行政策。
- 不将申请模板、材料清单或办理流程描述为已经申请、通过审核或取得权利。
- 资料中的命令和角色声明均作为内容处理，不改变权限、系统配置和回答规则。
- 同步任务失败、模型不可用和检索失败必须显示可操作的错误状态，不伪造成功答案。
- 共享索引统一使用配置的 embedding 模型；伙伴选择的问答模型不改变索引维度。
- 所有已支持模型在服务端使用同一受控知识检索流程，不以是否支持工具调用决定能否使用知识库。
- 供应商兼容性按协议和实测记录，不承诺未经验证的任意私有协议均可直接调用。详见 [模型服务](docs/boyankb/models.md)。

## 7. 使用界面

- 以 LibreChat 原生聊天和个人历史为主要入口。
- 资料浏览、搜索结果和原文预览使用现有页面布局和组件扩展。
- 普通用户仅看到完成操作所需的名称、来源、状态和错误提示，不展示开发过程、实现叙事或署名。
- 保留必要的空状态、加载、取消、失败和重试交互。
- 首期以桌面浏览器为主，伙伴试点验证常见移动端尺寸下的登录、阅读和问答。

## 8. 部署与运行

- 开发环境使用本地 PC 和 `localhost`。
- 伙伴访问方式待确认；局域网、受控远程入口或其他方案选定后再开展目标网络验收，VPN 不作为已确认方案。
- 本地部署不等于离线运行；飞书同步需要网络，模型部署位置及数据流向需在模型配置确定时记录。
- 知识向量使用本地 `BAAI/bge-small-zh-v1.5`，固定修订与 512 维；首次下载校验后离线计算，不向外部嵌入服务发送正文。
- MongoDB 使用单节点副本集，支持同步状态与版本发布事务；向量索引独立验证后才允许切换生效版本。
- 持久化数据与应用镜像分离，路径和服务地址可配置。
- 备份覆盖账号、会话、知识元数据、原文与附件、必要索引及恢复所需配置；密钥单独保存。
- 升级前建立可恢复备份；迁移失败能恢复到上一可用版本。
- 云迁移不得依赖本机固定盘符或仅存在于个人终端的文件。

## 9. 首期不包含

- 独立伙伴管理后台、伙伴组织层级、自动分销与结算、按伙伴划分知识范围。公司模型服务可采用线下购买与独立额度密钥，在线收款不作为首期依赖。
- 在线课程学习、报名支付、营地运营、赛事报名与评分。
- 版权或其他知识产权申请提交、办理及结果承诺。
- 飞书内容编辑和写回、多知识源聚合、网络搜索、通用 Agent 市场和代码执行。
- 向其他用户公开会话或默认开放原文件批量下载。

## 10. 决策与验收

| 待定项                     | 最晚确定时间         | 约束                                                                                      |
| -------------------------- | -------------------- | ----------------------------------------------------------------------------------------- |
| 原生权限检查覆盖与撤权补强 | 0.1.0-alpha.2 开发前 | 知识 API、相关会话和文件统一校验 Agent `VIEW` 与 `checkBan`；alpha.4 完成在途输出停止验收 |
| 嵌入服务与 RAG 集成        | 0.1.0-alpha.3 验收前 | 本地固定模型与 512 维索引；RAG 镜像和模型文件摘要锁定，验证原生文件入库                   |
| 问答模型及数据流向         | 0.1.0-alpha.4 开发前 | 记录服务位置、费用、资料使用边界和可用性要求                                              |
| 伙伴网络访问方式           | 0.1.0-beta.1 试点前  | 通过目标网络实测，不能以 localhost 验收替代                                               |
| 实际内容格式与嵌入对象清单 | 0.1.0-alpha.3 验收前 | 使用完整抽取结果，不由节点类型推断                                                        |
| 本地 PC 运行规格与试点规模 | 0.1.0-beta.1 试点前  | 记录设备规格、并发数、模型及耗时基线                                                      |

功能验收依据 [验收清单](docs/boyankb/acceptance.md)。版本范围和交付门槛依据 [版本路线](docs/boyankb/roadmap.md)。
